# Helm values for the upstream Istio base chart
# (https://istio-release.storage.googleapis.com/charts, chart: base).
# Installs CRDs and cluster-wide resources only — no workloads, so none of
# the image-pull constraints that apply to istiod/other charts apply here.

defaultRevision: default
